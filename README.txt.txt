2019-16600 정희수
<컴파일 환경>
visual studio 2019 
windows 10 

----------------------------------------------------
1. >이후에 코드 입력하기 
2. memory가 꽉 차면 *****garbage collect***** 메시지 출력
3. 이미 많은 함수들이 정의되어 있어서 garbage collect를 한 이후에도 memory가 충분치 않으면 memory full 메시지를 출력하고
exit한다.
4.  cond에 else문이 없고, else문이 실행되는 상황이라면 error msg를 내고 새로운 인풋을 받는다.  
5. 빈 스트링을 입력하거나, define 문을 실행하면 아무것도 출력되지 않고 다음 인풋을 받는다.
----------------------------------------------------


memory tree, hash table을 보고 싶다면 
_memory_를 입력하면 된다. 

memory size - 100
hash size - 500   
